# Gradle
.gradle/
build/
*/build/
out/
*/out/

# IDE
.idea/
*.iml
*.ipr
*.iws
.vscode/
*.classpath
*.project
*.settings/

# OS
.DS_Store
Thumbs.db

# Forge decompile cache
run/
fg_cache/

# Logs
*.log

# Compiled artifacts - keep the source, not the builds
*.jar
!gradlew.bat

# Translator
pklogin-translator/translator-config.yml
pklogin-translator/cache.json

# Compiled classes checked in by mistake
*.class

# Local tooling, not part of the plugin. analyzer.py dumps the whole source
# tree into all_code.txt, which makes grepping the repo return every hit twice.
analyzer.py
all_code.txt

# AI assistant workspace. Machine-local: absolute paths, per-session ids and a
# vendored copy of skill definitions that belong to whoever set them up.
.claude/
CLAUDE.md
