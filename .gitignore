# Gradle
.gradle/
build/
*/build/
out/
*/out/

# IDE
.idea/
*.iml
*.ipr
*.iws
.vscode/
*.classpath
*.project
*.settings/

# OS
.DS_Store
Thumbs.db

# Forge decompile cache
run/
fg_cache/

# Logs
*.log

# Compiled artifacts - keep the source, not the builds
*.jar
!gradlew.bat

# Translator
pklogin-translator/translator-config.yml
pklogin-translator/cache.json

# Compiled classes checked in by mistake
*.class

# Whole-source dump produced by analyzer.py; it duplicates the tree and makes
# grepping the repo return every hit twice.
all_code.txt

# Per-machine Claude Code permissions. Holds absolute paths and per-session
# scratchpad ids, so it is rewritten constantly and means nothing on another
# checkout. The shared settings.json is tracked.
.claude/settings.local.json
